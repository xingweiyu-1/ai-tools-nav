{
  "name": "ai-tools-nav",
  "version": "1.0.0",
  "description": "AI 编程工具聚合导航站 —— 收录 30+ 个 AI 编程工具，支持全文模糊搜索，GitHub Actions 定时同步仓库数据，部署于 Cloudflare Pages（零运行时依赖）。",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync": "node scripts/sync-repos.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "fuse.js": "^7.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "gray-matter": "^4.0.3",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
